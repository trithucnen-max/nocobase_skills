target: $SELF.table.actions.addNew
mode: drawer
blocks:
  - key: createForm
    type: createForm
    coll: nb_crm_data_analysis
    fields:
      - name
      - collection
      - description
      - sql
    field_layout:
      - - name
      - - collection
      - - description
      - - sql
    actions:
      - submit
