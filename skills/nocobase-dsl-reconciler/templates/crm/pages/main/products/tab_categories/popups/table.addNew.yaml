target: $SELF.table.actions.addNew
mode: dialog
blocks:
  - key: createForm
    type: createForm
    coll: nb_crm_product_categories
    fields:
      - name
      - description
      - sort_order
    field_layout:
      - - name
      - - description
      - - sort_order
    actions:
      - submit
