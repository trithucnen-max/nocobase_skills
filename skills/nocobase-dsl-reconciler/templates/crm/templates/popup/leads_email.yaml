uid: 53hvsa4ggul
name: Leads Email
type: popup
collectionName: nb_crm_leads
dataSourceKey: main
targetUid: 6im0lrahx9q
content:
  blocks:
    - key: mailMessages
      type: mailMessages
      coll: mailMessages
      dataScope:
        logic: $or
        items:
          - path: from
            operator: contains
            value: '{{ ctx.popup.record.email }}'
          - path: to
            operator: contains
            value: '{{ ctx.popup.record.email }}'
      actions:
        - filter
        - refresh
        - addNew
        - type: ai
          employee: ellis
          tasks_file: ./ai/leads_email_mailMessages_tasks.yaml
  layout:
    - - mailMessages
