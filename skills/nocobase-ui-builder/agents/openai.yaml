interface:
  display_name: "NocoBase UI Builder"
  short_description: "INTERNAL ONLY: Use after Portal Manager selects no-code"
  default_prompt: "$nocobase-ui-builder:INTERNAL;Gate:PMfirst:multiPortal:false`nb api flow-surfaces`Intent-firstwholePage1 tabcomplex multi-blocknested popupmulti-reactionreadsmetadataokapply-blueprintlocalized editsbackend actionsget-reaction-metafirst writeapplyBlueprintno createMenu/createPage/compose/configure/updateSettings/add*/move*/remove*/set*Rulesfail->repair aggregate errors+retry<=5success only explicit local/live gapreportRepeat-eligiblecontextual list-templateskeyword-only searchdiscovery-onlylocal customizationtemplate sourcefirst resultpage wording≠local-only intentask, not copysame-titlerouteIdpageSchemaUidsame-groupsame-titlereplacedifferent-groupsame-titlenomergesemantic AntDesign iconmulti-non-filter keyed layout/titlestemplates exemptsingle non-filterhostBound搜索/filtersameHostfilterActionoptionalunless askedsetLayoutstring[][]number[][[a],[b]]2col[[a,b]]stackuid not keysearchPage≠filterfilter/search blockfilterForm4+ fieldscollapserow1defaultFilteromitbackend4explicitemptyinvalid<4errorsraw payload onlytreeTableomitrecordActionsdefaultaddChildEFmoveNoInjectfirstlivedirectnon-assoctitleField>name>code>title分析看板KPIJSBlocktrenddistributionrankingpercentage占比 chartrequiredtable/list!=chartchartassets.charts+block.chartcardGridsettings.codeconfigurechanges.code/versionban(top-level code/version,changes.settings,stepParams禁,propsdecoratorPropsflowRegistry)typejsBlockonlykanban/pipeline/status columnsKanbanassociatedRecords+associationField1 editFormtryTemplatesaveAsTemplate"

# Long-form guardrails kept here for docs-consistency checks and human review:
# rebuild defaults.collections from scratch via live metadata
# defaults.collections fieldGroups >10 effective fields
# defaults fieldGroups self-review approve/regenerate once lowest reasoning no-think no chain-of-thought
# fixed view/addNew/edit
# table addNew threshold
# associations popups{name,description} first-segment
# described generated add/edit fields => structured formBehavior or formBehaviorDescriptionReview.fields.<field>{decision,reasonCode}; no legacy review/empty-object fallback
# Portal preflight before every mutation: one Portal or isDefault marker is not enough; require selected record portalType=no-code. A sole AI Portal exits UI Builder but immediately continues the same request through Portal Manage's source project, with Portal Manage pulling first when no development path exists, without asking whether to use it or create no-code. Missing/unsupported types return to Portal Manage; no-code requires a non-empty selected name and exact kind=portal + routeName mapping.
# Mandatory entry: before any other UI Builder instruction, load and execute nocobase-portal-manage for the same request. Reuse an existing current-request Portal Manager outcome so no-code dispatch does not loop.
# Use the unique target portalUid/uid as navigation.portalUid; ignore target default, layout targets, and backing layoutUid. Legacy Admin/Mobile layout requires capabilities.multiPortal=false.
# navigation-portal-type-unsupported, navigation-portal-selection-required, navigation-portal-not-found, and navigation-admin-layout-not-portal-target stop or hand off; never repair-loop or retry against Admin.

dependencies:
  tools:
    - type: "cli"
      value: "nb"
      description: "After Portal Manage resolves one no-code Portal, map kind=portal plus exact routeName and use its portalUid for nb api flow-surfaces; use direct Admin/Mobile only when multiPortal=false"

policy:
  allow_implicit_invocation: false
