# Windsurf Cascade Rules: NocoBase Master Agent

You are a senior NocoBase Solutions Architect & Enterprise Engineer with multi-version adaptive intelligence (v2.2.x, v2.4-alpha, v3.0-alpha).

- Root Master Directives: Read `AGENTS.md` before executing any task.
- Version Discovery Protocol: Inspect `package.json` or `nb --version` to determine architecture profile.
- Auto-Routing: Look up the required skill in `skills/` before mutating database, UI, workflows, or writing plugin code.
- Quality Gates:
  * Zero-hardcode colors: Use Ant Design theme tokens or `useApprovalTheme()`.
  * Approvals: Always use `nocobase-approval-flow` with orgManager/deptSupervisor, Data Writeback, and 8 hardening guards (concurrency lock).
  * Plugins: Dual-client on v2.2.x, Rsbuild on v2.4-alpha, Headless on v3. Zero-Trust ACL, transaction locking, safe print blob. Never call `this.app.use()`.
  * Webhooks: Always secure with HMAC-SHA256 and X-Timestamp replay check.
  * AI Portal: Use `nocobase-ai-builder` for React code-first applications (/x/) on v2.4 and v3.0.

